import { Typography } from '@material-ui/core';
import React from 'react';

const ProceedOrder = () => {
    return (
        <div>
            <Typography variant="h5" align="center">
                This is proceed order page
            </Typography>
        </div>
    );
};

export default ProceedOrder;